#include "main.h"
/**
* rev_string - reverses a string in place
* @s: the string to be reversed
*/
void print_rev(char *s)

{
int i = 0;

while (i != '\0')
{
i++;
}

i--;

while (i >= 0)
{
	_putchar(str[i]);
	i--;
}


_putchar('\n');
}
